Vagrant-rails sets up a vagrant box for rails development, with ruby 2.0 and the latest stable rails.

Usage
=====

Clone this repo. Run `vagrant up` to initialize the virtual machine. Start coding in `/vagrant`.
